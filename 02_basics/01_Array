//  Array

// the array, which is a collection of elements.
// it is zero indexing
// Shallow copy = share same copy


// const myArray = [ 1,2,3,4]
var myArray = [1, "Hello", [2, 3]];
console.log(myArray[0]);

const myHeroes = [ "IronMan", "Spiderman" ];
console.log(myHeroes[1]);
